let n = parseInt.gets().split();
let min = parseInt(gets().split());
let max = parseInt(gets().split());


   let altura, conta = 0;
		for (let x=0 ; x<n ; x++) {
            if( altura >= vet[1]  && altura <= vet[2])
                conta = conta++;
    
            }
  		print(conta);
        